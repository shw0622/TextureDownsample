# 开发期配置：本项目暂不打包安装（still in development）。
# 在项目根目录用 `python -m pytest` 跑测试；pythonpath=. 让测试无需安装即可
# import dpid_lean。需要依赖时用 `pip install -r requirements.txt`。

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
